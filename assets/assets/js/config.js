/* =========================================================
   Avaliação Digital 2.0 — Configuração
   ========================================================= */

window.APP_CONFIG = {
  // URL da função serverless na Vercel (backend real, Supabase por trás).
  API_URL: "https://conexao-brown.vercel.app/api/action",

  // Evento padrão desta instalação — hoje só usado pela tela do telão
  // (telao.html), que ainda não navega por múltiplos eventos. Troque
  // pelo ID do evento real assim que ele existir no Painel do Administrador.
  EVENT_ID: "demo-2027",

  APP_NAME: "Avaliação Digital 2.0",
  ORG_NAME: "Conexão Junina"
};
